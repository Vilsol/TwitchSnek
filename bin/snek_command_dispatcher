#!/usr/bin/env python
import os
import sys

dir_path = os.path.dirname(os.path.realpath(__file__))
sys.path.insert(0, dir_path + "/..")

from lib.snek_command_dispatcher import SnekCommandDispatcherComponent

SnekCommandDispatcherComponent.run()
